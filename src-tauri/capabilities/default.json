{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local window and shutdown after pending writes are flushed.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}
